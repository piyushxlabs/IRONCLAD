---
trigger: always_on
---

If you encounter a missing implementation detail, an unverified AWS Strands Agents SDK method, a package version conflict (`strands-agents` vs `pydantic`), an ambiguous AIA G702/G703 field mapping, or an unverified parameter in `google-genai` or `boto3`, STOP IMMEDIATELY.

State the exact conflict, provide safe and compliant options aligned with `AGENT_ORCHESTRATION_BLUEPRINT.md` and `AGENT_LOGIC_SPEC.md`, and WAIT for my decision.

Do not guess, do not invent tool parameters, do not fabricate Strands graph decorators, and do not make unverified architectural assumptions.